// AUTO-GENERATED by typelayout-compat toolchain — do not edit manually.
// This file exports TypeLayout signatures for the types defined in the user header.

#include <boost/typelayout/tools/sig_export.hpp>
#include "@TYPES_HEADER@"
#include <string>
#include <filesystem>

int main(int argc, char* argv[]) {
    boost::typelayout::SigExporter ex;

@TYPE_REGISTRATIONS@

    if (argc >= 2) {
        std::string dir = argv[1];
        std::filesystem::create_directories(dir);
        std::string path = dir;
        if (path.back() != '/') path += '/';
        path += ex.platform_name() + ".sig.hpp";
        return ex.write(path);
    }
    ex.write_stdout();
    return 0;
}
